<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:Dari.App.ViewModels"
        x:Class="Dari.App.Views.ChecksumErrorView"
        x:DataType="vm:ChecksumErrorViewModel"
        Title="{DynamicResource Checksum.Title}"
        Width="460"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        CanResize="False"
        ShowInTaskbar="False">

    <Grid RowDefinitions="*,Auto" Margin="20">

        <StackPanel Grid.Row="0" Spacing="8" VerticalAlignment="Center">
            <TextBlock Text="{DynamicResource Checksum.Prompt}"
                       TextWrapping="Wrap" />
            <TextBlock Text="{Binding EntryPath}"
                       FontFamily="Monospace"
                       TextWrapping="Wrap"
                       Opacity="0.8" />
            <TextBlock Text="{Binding Detail}"
                       TextWrapping="Wrap"
                       Foreground="Red"
                       FontSize="11"
                       Margin="0,4,0,0" />
            <TextBlock Text="{DynamicResource Checksum.Continue}"
                       TextWrapping="Wrap"
                       Margin="0,4,0,0" />
        </StackPanel>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Spacing="8"
                    Margin="0,16,0,0">
            <Button Content="{DynamicResource Button.Abort}"
                    Command="{Binding AbortCommand}" />
            <Button Content="{DynamicResource Button.Continue}"
                    Classes="accent"
                    Command="{Binding ContinueCommand}" />
        </StackPanel>

    </Grid>

</Window>
