namespace Dari.Archiver;

/// <summary>Public metadata about the Dari archive format and library.</summary>
public static class DariInfo
{
    /// <summary>Maximum archive format version supported by this build of the library.</summary>
    public const byte MaxFormatVersion = Format.DariConstants.FormatVersion;
}
